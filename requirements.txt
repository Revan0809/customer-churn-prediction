# Environment for EDA + training + explainability (notebooks/, src/).
# For running the API only, see backend/requirements.txt.
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
imbalanced-learn==0.12.3
xgboost==2.0.3
shap==0.45.1
matplotlib==3.8.4
seaborn==0.13.2
joblib==1.4.2
jupyter==1.0.0

# SQL + BI analytics layer (sql/, dashboard/) -- sqlite3 is Python stdlib,
# no extra dependency needed for the sql/ scripts themselves.
streamlit==1.60.0
altair==6.2.2
